<h1>Sign up</h1>

<%#= semantic_form_for @user do |f| %>
<%= form_for @user do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit "Sign up" %>
	
  </div>  
<% end %>
